@model Vidly3.Models.Movie
@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>@Model.Name</h2>

<ul>
    <li>Genre: @Model.Genre.Name</li>
    @if (Model.ReleaseDate.HasValue)
    {
        <li>Release Date: @Model.ReleaseDate.Value.DayOfWeek, @Model.ReleaseDate.Value.ToLongDateString()</li>
    }
    @if (Model.DateAdded.HasValue)
    {
        <li>Date Added: @Model.DateAdded.Value.DayOfWeek, @Model.DateAdded.Value.ToLongDateString()</li>
    }
    <li>Number in Stock: @Model.NumberOfStock</li>
</ul>